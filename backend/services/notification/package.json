{
  "name": "@adbar/notification",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "nest build notification",
    "start": "nest start notification",
    "start:dev": "nest start notification --watch",
    "start:debug": "nest start notification --debug --watch",
    "start:prod": "node dist/services/notification/main",
    "lint": "eslint \"{src,apps,libs,test}/**/*.ts\"",
    "test": "jest --config ./services/notification/jest.config.ts"
  }
}
