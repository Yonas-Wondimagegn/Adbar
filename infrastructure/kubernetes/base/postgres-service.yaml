apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: adbar
  labels:
    app: postgres
    app.kubernetes.io/part-of: adbar
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
  selector:
    app: postgres
