apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: adbar
  labels:
    app: redis
    app.kubernetes.io/part-of: adbar
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
      name: redis
  selector:
    app: redis
