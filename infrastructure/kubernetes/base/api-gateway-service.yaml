apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: adbar
  labels:
    app: api-gateway
    app.kubernetes.io/part-of: adbar
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: api-gateway
